import FlagCount from "./FlagCount";
import TurnCount from "./TurnCount";
function Header() {
  return (
    <section>
      <FlagCount />
      <TurnCount />
    </section>
  );
}

export default Header;